{
  "name": "workflow",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "npm run clean && esbuild src/index.ts --bundle --sourcemap --outfile=dist/index.mjs --platform=node --target=esnext --format=esm --external:@aws/durable-execution-sdk-js --external:@aws-sdk/client-dynamodb --external:@aws-sdk/lib-dynamodb --external:@aws-sdk/client-sqs --external:@aws-sdk/client-sns",
    "test": "cross-env NODE_OPTIONS=\"$NODE_OPTIONS --experimental-vm-modules\" jest --config jest.config.mjs"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.943.0",
    "@aws-sdk/client-sns": "^3.946.0",
    "@aws-sdk/client-sqs": "^3.946.0",
    "@aws-sdk/lib-dynamodb": "^3.943.0",
    "@aws/durable-execution-sdk-js": "^1.0.0"
  },
  "devDependencies": {
    "@aws/durable-execution-sdk-js-testing": "^1.0.0",
    "@types/sinonjs__fake-timers": "^15.0.1",
    "cross-env": "^10.1.0",
    "esbuild": "^0.27.1",
    "jest": "^30.2.0",
    "rimraf": "^6.1.2",
    "ts-jest": "^29.4.6"
  }
}
